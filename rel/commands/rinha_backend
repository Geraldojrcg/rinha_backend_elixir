#!/bin/sh

case $1 in
  migrate)
  echo "Running Migrations"
  /app/bin/rinha_backend eval "Release.migrate()"
  ;;
  create_db)
  echo "Creating Database"
  /app/bin/rinha_backend eval "Release.create_db()"
  ;;
  *)
  /app/bin/rinha_backend $@
  ;;
esac